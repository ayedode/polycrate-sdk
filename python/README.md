# Python client

Package `polycrate` (`python/polycrate`). Not on PyPI — install from GitHub:

```
pip install "polycrate @ git+https://github.com/ayedode/polycrate-sdk.git@v<api-version>#subdirectory=python/polycrate"
```

Install, authentication, and usage are in the [repository README](../README.md#python).
