# JavaScript / TypeScript client

Package `@ayedo/polycrate-sdk`. Not on npm — install from GitHub:

```
npm i github:ayedode/polycrate-sdk#v<api-version>:js
```

Install, authentication, and usage are in the [repository README](../README.md#javascript--typescript).
